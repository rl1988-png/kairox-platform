services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-kairox}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-kairox_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-kairox}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-kairox} -d ${POSTGRES_DB:-kairox}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-kairox}:${POSTGRES_PASSWORD:-kairox_dev_password}@postgres:5432/${POSTGRES_DB:-kairox}
      REDIS_URL: redis://redis:6379/0
      KAIROX_RUN_MIGRATIONS: ${KAIROX_RUN_MIGRATIONS:-true}
      KAIROX_MIGRATION_ATTEMPTS: ${KAIROX_MIGRATION_ATTEMPTS:-20}
      KAIROX_MIGRATION_RETRY_SECONDS: ${KAIROX_MIGRATION_RETRY_SECONDS:-3}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres_data:
